type ErrorMessages = {
    shipments: string;
    events: string;
  };

const errorMessages: ErrorMessages = {
    shipments: 'Sorry an error has occured, please try again later',
    events:'Sorry an error has occured, please try again later'
}

export default errorMessages